# Hooka `1.0.0`

`1.0.0` is the first stable release of Hooka as a generic, internet-exposed automation control plane.

Hooka's supported operating model for this release is:

- single node
- self-hosted deployment
- SQLite-backed queue and state store

## Included in `1.0.0`

- retry with exponential backoff
- dead-letter queue support via `dead-lettered`
- stuck run recovery based on worker lease expiry
- target-backed webhook execution with policy validation and artifact readiness checks
- admin bearer token protection for read/admin APIs
- in-memory per-client and global rate limiting for `/api/*`
- SSE live updates for run events and worker heartbeats through short-lived tickets
- target APIs, CLI commands, and admin UI panels
- audit event storage and CLI/UI visibility
- file-backed target CRUD plus scaffold templates for common operator flows
- trusted-proxy-aware rate limiting guidance for reverse-proxy deployments
- request body limits, CSP headers, executor timeouts, and machine-readable OpenAPI output
- retention cleanup through `hooka cleanup` and worker-side sweep support

## Image tags

Mutable tags continue to publish from `main`:

- `ghcr.io/imjlk/hooka:webhook-server`
- `ghcr.io/imjlk/hooka:cf-pages`
- `ghcr.io/imjlk/hooka:cf-cache`
- `ghcr.io/imjlk/hooka:wp-ops`
- `ghcr.io/imjlk/hooka:wp-wrangler`

Stable immutable tags publish from `v1.0.0`:

- `ghcr.io/imjlk/hooka:1.0.0-webhook-server`
- `ghcr.io/imjlk/hooka:1.0.0-cf-pages`
- `ghcr.io/imjlk/hooka:1.0.0-cf-cache`
- `ghcr.io/imjlk/hooka:1.0.0-wp-ops`
- `ghcr.io/imjlk/hooka:1.0.0-wp-wrangler`

## GHCR retention

- mutable tags remain rolling pointers from `main`
- stable immutable `1.x.y-*` tags remain permanent rollback points
- prerelease immutable tags such as `1.0.0-rc.1-*` are pruned only after the matching GA release is published
- cleanup only affects GHCR aliases and does not remove Git tags or release notes

## Minimum env for public deployment

- `HOOKA_WEBHOOK_SECRET`
- `HOOKA_ADMIN_TOKEN`
- `HOOKA_TARGETS_PATH`

For shared-volume Pages deploys, the worker also needs:

- `CLOUDFLARE_API_TOKEN`
- `CLOUDFLARE_ACCOUNT_ID`

## Operational notes

- Retryable failures are requeued with backoff until `maxAttempts` is exhausted.
- Retryable failures that exceed `maxAttempts` become `dead-lettered`.
- Validation, policy, and auth failures are terminal and non-retryable.
- SSE clients obtain a short-lived ticket from `POST /api/events/ticket` before opening `/api/events/stream`.
- API CORS remains same-origin by default unless `HOOKA_CORS_ORIGINS` is set.
- `hooka cleanup` prunes old terminal runs and audit events, and workers can perform scheduled retention sweeps.
- Targets are file-backed by default at `.hooka/targets.json`.
- Existing generic and compatibility webhook contracts remain supported.

## Deferred after `1.0.0`

- planned preset promotion
- richer target authoring flows beyond scaffold + JSON editor
- token rotation and stronger proxy-chain controls
- multi-node/Postgres support
